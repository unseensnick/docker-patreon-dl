# Copy this file to .env and customize as needed.
# All values shown are the defaults — only uncomment and change what you need.

# ── Display ──────────────────────────────────────────
# RESOLUTION=2560x1440        # 1920x1080, 2560x1440, 3840x2160
# DPI=144                     # 96, 120, 144
# COLOR_DEPTH=24              # 16 (less bandwidth), 24 (full color)

# ── Access ───────────────────────────────────────────
# VNC_PORT=5900               # Host port for VNC
# NOVNC_PORT=6080             # Host port for noVNC web interface
# VNC_PASSWORD=               # Empty = no password

# ── System ───────────────────────────────────────────
# TZ=UTC                      # e.g. America/New_York, Europe/London

# ── Resource Limits ──────────────────────────────────
# SHM_SIZE=4gb                # Shared memory for Electron/Chromium
# MEM_LIMIT=4g                # Container memory cap
# CPUS=4.0                    # CPU core limit
# PIDS_LIMIT=512              # Max number of processes

# ── Tool Paths (rarely need changing) ────────────────
# FFMPEG_PATH=/usr/bin/ffmpeg
# YTDLP_PATH=/usr/local/bin/yt-dlp
# DENO_PATH=/usr/local/bin/deno
